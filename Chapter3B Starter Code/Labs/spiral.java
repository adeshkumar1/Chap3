package Labs;
import TurtleGraphics.*;
import java.lang.*;
import java.util.*;

public class spiral
{
    public static void main (String [] args)
    {
        StandardPen pen = new StandardPen();
        int x = 0, y = 0;
        for (int length = 0; length < 1000; length+=10)//keeps increasing length and turning until length == 1000
        {
            pen.move(length);
            pen.turn(90);
        
            
        
        }
    
    
    
    
    
    
    }
}
